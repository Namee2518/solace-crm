# Solace CRM — Frontend

React (Vite) frontend for the Solace CRM application. See the [project root README](../README.md) for full setup instructions, API docs, and environment variables.

## Quick start

```bash
npm install
cp .env.example .env
npm run dev
```
